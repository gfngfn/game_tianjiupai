module Quad = struct

  type seat =
    | Seat0
    | Seat1
    | Seat2
    | Seat3

  type t<$a> = {$a, $a, $a, $a}

  val to_list<$a>(quad : t<$a>) : list<$a> =
    let {x0, x1, x2, x3} = quad in
    [x0, x1, x2, x3]

end
