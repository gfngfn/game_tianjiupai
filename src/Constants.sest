module Constants = struct

  val disconnection_timeout() =
    30000  /* 30 seconds */

  val maximum_num_innings() =
    8

  val room_expiration_timeout() =
    1000 * 60 * 60  /* one hour */

  val user_expiration_timeout() =
    1000 * 60 * 60  /* one hour */

  val maximum_num_rooms() =
    1000

  val maximum_num_users() =
    2

  val maximum_num_rooms_per_user() =
    2

end
